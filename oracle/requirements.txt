# SAGE Oracle - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0          # Required for video processor in transformers
transformers>=4.40.0
accelerate>=0.26.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Video processing
av>=10.0.0,<16              # PyAV for video decoding (<16: 16.x ships sdist-first;
                            #  source builds need ffmpeg dev headers)
qwen-vl-utils>=0.0.8        # Qwen VL utilities

# Optional: Visualization (install with: pip install -r requirements.txt[viz])
matplotlib>=3.7.0           # Timeline visualization
opencv-python>=4.8.0        # Video overlay visualization



# Optional: Evaluation statistics
scipy>=1.10.0               # Spearman correlation, Gaussian blur fallback

# Release tooling (HF dataset staging/verification + Tier-1 reproduction)
# (API-oracle providers + croissant validation live in requirements-optional.txt)
pyarrow>=14.0.0             # parquet staging + verification
datasets>=2.19.0            # load_dataset for the released corpus
huggingface_hub>=0.23.0     # upload / download / verify
